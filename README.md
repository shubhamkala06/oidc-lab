# oidc-lab
A complete OpenID Connect (OIDC) Authorization Code Flow implementation in Node.js without relying on OIDC client libraries such as openid-client. The project demonstrates the protocol internals, including discovery, authorization, token exchange, state validation, JWKS retrieval, JWT signature verification, and ID token claim validation.
